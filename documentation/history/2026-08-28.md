# 2026-08-28: Catalog alignment + declarative settings port

## Accomplished

Two-commit fleet-review pass (obsidian-plugin-template is the canonical source):

1. **Catalog reviewer alignment (A-E)**: CHANGELOG.md and eslint.config.ts are no longer export-ignored (the catalog reviewer builds the git archive); the changelog reaches the bundle through the `__PLUGIN_CHANGELOG__` build define instead of a markdown import attribute. The eslint config now extends the full `eslint-plugin-obsidianmd` recommended preset — the previous config hand-picked six rules, so sentence-case and most catalog rules never ran despite 0.4.1 being installed. Type roots pinned (`@types/bun` 1.4.0, `@types/node`, tsconfig `types: ["bun", "node"]`), obsidian typings 1.13.1. `bun audit` clean: 30 advisories (1 critical handlebars JS injection) cleared via in-range updates plus overrides (handlebars 4.7.9, lodash 4.18.1, tmp 0.2.7). Toolchain synced with the template: bun 1.4.0, `bun test --isolate`, CI audit gate, release.sh with the unattended flags. The debug logger's info level routes through `console.debug` (console.info counts as unnecessary logging for the reviewer).

2. **Declarative settings port (feat!, minAppVersion 1.8.7 → 1.13.0)**: `getSettingDefinitions()` replaces the 376-line `display()` tab. The exclusion lists are native `type: 'list'` definitions with an inline suggester add-row (top-level — a group's `items` cannot host a list); the API key is a masked render row; the regenerate action is a button row placed after the lists as before. New serialized persist-then-commit `updateSettings(mutator)` (immer) replaces the optimistic `updateSetting`; side effects (debug-mode flag, village regeneration) run strictly after a successful commit. `setControlValue` rejects type mismatches, non-integer/out-of-range numbers, out-of-enum dropdown values and unknown keys.

## Verification

- 417 tests green (15 new settings-write tests; persist-then-commit ordering and write serialization mutation-checked — deliberately regressed implementations fail them), tsc clean, `lint --max-warnings 0` clean, build green, archive simulation (git archive → install → build) green.
- Live vault check: declarative pane renders (groups, sliders, dropdowns, password field, list empty states, support block in block flow); `villageSeed` round-trip persisted; `#TestTag` normalized to `testtag` and persisted; list entry rendered with a working Delete button (memory + disk cleared); type-mismatch, `__proto__` and out-of-enum writes rejected with memory intact.
- Adversarial review (gpt-5.6-sol xhigh): the one High ("list entries with neither control nor render are skipped") was REFUTED live — inside `type: 'list'`, name-only items render as full rows with delete affordances; the skip rule applies to plain group rows. Fixed from the review: integer + range validation on the numeric writes, no failure re-sync on the API-key input (a re-sync could roll the input back underneath newer queued writes), duplicate exclusions clear the input again (old-tab parity), regenerate row restored to its old position, `readChangelogDefine` test coverage, this history entry and the README's minimum-version note.

## Decisions

- `@typescript-eslint/no-deprecated` stays off (fleet-consistent with journal-base and life-tracker); the deprecated `display()`/`setDynamicTooltip` uses are gone with the port.
- `scripts/**` is lint-ignored per the fleet-standard config; the build scripts keep their own spec coverage instead.
